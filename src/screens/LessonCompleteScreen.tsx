import type { Lesson } from "~/data/latinLessons";
import NavBar from "~/components/NavBar"; import LessonComplete from "~/components/LessonComplete";
interface Props {lesson:Lesson;totalLessons:number;correct:number;total:number;isLastLesson:boolean;onNext:()=>void;onRestart:()=>void;onBack:()=>void;onOpenAIPractice:(lessonId:number)=>void;onOpenDrill:()=>void}
export default function LessonCompleteScreen({lesson,totalLessons,correct,total,isLastLesson,onNext,onRestart,onBack,onOpenAIPractice,onOpenDrill}:Props){return <div className="min-h-dvh flex flex-col"><NavBar/><main className="flex-1 px-4 py-4 sm:py-8 flex items-center justify-center"><div className="mx-auto w-full max-w-md"><LessonComplete lessonNumber={lesson.id} totalLessons={totalLessons} correct={correct} total={total} onNext={onNext} onRestart={onRestart} isLastLesson={isLastLesson} onDrill={onOpenDrill}/><div className="text-center mt-6"><button onClick={()=>onOpenAIPractice(lesson.id)} className="mb-2 w-full max-w-xs rounded-xl bg-purple-100 border-2 border-purple-300 py-2.5 text-sm font-bold text-purple-800 hover:bg-purple-200 transition">🤖 AI Practice on This Lesson</button><button onClick={onBack} className="text-sm text-gray-400 hover:text-burgundy-600 transition block mx-auto">← Back to All Lessons</button></div></div></main></div>}
